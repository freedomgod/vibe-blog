{
  "buildCommand": "curl -LsSf https://astral.sh/uv/install.sh | sh && uv pip install -e .",
  "framework": "flask",
  "regions": ["sfo1"],
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/index.py"
    }
  ]
}
